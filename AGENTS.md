# Project editing scope

- Default all requested changes to version A. Edit version B only when the user explicitly requests B.
- Preserve version B's page, model, API, and behavior. Scope A-specific styles under `.workbench-a`.
